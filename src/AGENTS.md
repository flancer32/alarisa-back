# Source

## Purpose

Contains TeqFW ECMAScript modules in the `Alarisa_Back_` namespace.

## Boundary

Keep reusable server-side responsibilities here. The self-hosted server composition root remains `@flancer32/alarisa`, and shared communication contracts belong to `@flancer32/alarisa-comm`.

Do not add placeholder modules merely to populate the namespace.
